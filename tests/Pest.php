<?php

declare(strict_types=1);

use Deinte\Peppol\Tests\TestCase;

// Use TestCase for Feature tests (requires Laravel)
uses(TestCase::class)->in('Feature');

// Unit tests don't need TestCase - they are self-contained
